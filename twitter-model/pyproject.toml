[project]
name = "twitter-model"
version = "0.1.0"
description = "A simplistic mini twitter model using SQL."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pytest>=8.0.0",
]


